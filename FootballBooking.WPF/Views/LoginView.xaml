<Window x:Class="FootballBooking.WPF.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FootballBooking.WPF.Views"
        xmlns:vm="clr-namespace:FootballBooking.WPF.ViewModels"
        mc:Ignorable="d"
        Title="Đăng nhập - Football Booking" Height="550" Width="450"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <Border CornerRadius="20" Background="{StaticResource SurfaceBrush}" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Color="Black" Opacity="0.5" ShadowDepth="0"/>
        </Border.Effect>
        
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Close Button -->
            <Button HorizontalAlignment="Right" VerticalAlignment="Top" 
                    Content="✕" Background="Transparent" Foreground="{StaticResource MutedTextBrush}"
                    BorderThickness="0" FontSize="18" Click="CloseButton_Click" Cursor="Hand"/>

            <!-- Header -->
            <StackPanel Grid.Row="1" Margin="0,0,0,40">
                <TextBlock Text="Football Booking" FontSize="28" FontWeight="Bold" 
                           Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="Chào mừng bạn quay trở lại" FontSize="14" 
                           Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>

            <!-- Form -->
            <StackPanel Grid.Row="2" VerticalAlignment="Center">
                <TextBlock Text="Email" Style="{StaticResource BaseText}" Margin="0,0,0,8"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource ModernTextBox}" Margin="0,0,0,20"/>

                <TextBlock Text="Mật khẩu" Style="{StaticResource BaseText}" Margin="0,0,0,8"/>
                <PasswordBox x:Name="PassBox" Style="{StaticResource ModernPasswordBox}" PasswordChanged="PassBox_PasswordChanged" Margin="0,0,0,10"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF5252" FontSize="12" 
                           HorizontalAlignment="Center" Margin="0,0,0,20"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}, FallbackValue=Collapsed}"/>

                <Button Content="ĐĂNG NHẬP" 
                        Command="{Binding LoginCommand}" Height="45" Margin="0,10,0,0">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource ModernButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                    <Setter Property="Content" Value="Đang đăng nhập..."/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <!-- Footer -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <TextBlock Text="Chưa có tài khoản?" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center"/>
                <Button Content="Đăng ký ngay" Background="Transparent" BorderThickness="0" 
                        Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" 
                        Margin="5,0,0,0" Command="{Binding RegisterCommand}" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
