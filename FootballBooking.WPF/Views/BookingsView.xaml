<UserControl x:Class="FootballBooking.WPF.Views.BookingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FootballBooking.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Lịch sử đặt sân" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

        <!-- Bookings List -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="10">
            <DataGrid ItemsSource="{Binding Bookings}" SelectedItem="{Binding SelectedBooking}"
                      AutoGenerateColumns="False" Background="Transparent" BorderThickness="0"
                      RowBackground="Transparent" GridLinesVisibility="None" IsReadOnly="True"
                      Foreground="White" FontSize="14" ColumnWidth="*">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#2A2A2A"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#333333"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Sân bóng" Binding="{Binding FieldName}" Width="2*"/>
                    <DataGridTextColumn Header="Ngày đặt" Binding="{Binding BookingDate, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
                    <DataGridTemplateColumn Header="Thời gian" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding StartTime, StringFormat={}{0:HH:mm}}"/>
                                    <TextBlock Text=" - "/>
                                    <TextBlock Text="{Binding EndTime, StringFormat={}{0:HH:mm}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalPrice, StringFormat={}{0:C}}" Width="*"/>
                    <DataGridTemplateColumn Header="Trạng thái" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#333333"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Confirmed">
                                                    <Setter Property="Background" Value="#2E7D32"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                    <Setter Property="Background" Value="#F57C00"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                    <Setter Property="Background" Value="#C62828"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Status}" FontSize="12" Foreground="White" FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Feedback & Actions -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="15" Margin="0,0,20,0">
                <StackPanel>
                    <TextBlock Text="Đánh giá lượt đặt sân" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding FeedbackMessage, UpdateSourceTrigger=PropertyChanged}" 
                                 Tag="Nhập nhận xét của bạn..." Height="60" AcceptsReturn="True"
                                 Style="{StaticResource ModernTextBox}" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Số sao (1-5):" FontSize="12" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding FeedbackRating}" Width="50" Style="{StaticResource ModernTextBox}"/>
                            <Button Content="GỬI" Style="{StaticResource ModernButton}" 
                                    Background="{StaticResource PrimaryBrush}" Margin="0,10,0,0"
                                    Command="{Binding SubmitFeedbackCommand}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <StackPanel Grid.Column="1" VerticalAlignment="Bottom">
                <Button Content="Làm mới" Style="{StaticResource ModernButton}" 
                        Background="Transparent" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}"
                        Foreground="White" Command="{Binding LoadBookingsCommand}" Margin="0,0,0,10"/>
                <Button Content="HỦY ĐẶT SÂN" Style="{StaticResource ModernButton}" 
                        Background="#C62828" Command="{Binding CancelBookingCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Loading Overlay -->
        <Grid Grid.Row="1" Background="#80000000" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Đang tải dữ liệu..." Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
