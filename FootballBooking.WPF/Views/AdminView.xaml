<Window x:Class="FootballBooking.WPF.Views.AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FootballBooking.WPF.Views"
        mc:Ignorable="d"
        Title="Admin Dashboard - Football Booking" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="{StaticResource SurfaceBrush}" Padding="20">
            <Grid>
                <TextBlock Text="ADMIN DASHBOARD" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                <Button Content="Làm mới dữ liệu" HorizontalAlignment="Right" 
                        Style="{StaticResource ModernButton}" Command="{Binding LoadAllDataCommand}"/>
            </Grid>
        </Border>

        <!-- Main Conte        <TabControl Grid.Row="1" Margin="20" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
                    <Setter Property="Padding" Value="20,10"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8,8,0,0" Margin="0,0,5,0">
                                    <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Fields Management -->
            <TabItem Header="Sân bóng">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="10">
                        <DataGrid ItemsSource="{Binding Fields}" SelectedItem="{Binding SelectedField}"
                                  AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0" Foreground="White">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Tên sân" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Giá/h" Binding="{Binding PricePerHour, StringFormat={}{0:C}}" Width="*"/>
                                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <ScrollViewer Grid.Column="1" Margin="20,0,0,0">
                        <StackPanel>
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                                <StackPanel>
                                    <TextBlock Text="Cập nhật giá" FontWeight="Bold" Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding SelectedField.Name, FallbackValue='Chưa chọn'}" Margin="0,0,0,10"/>
                                    <TextBox Text="{Binding NewFieldPrice}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>
                                    <UniformGrid Columns="2">
                                        <Button Content="LƯU" Style="{StaticResource ModernButton}" Command="{Binding UpdateFieldPriceCommand}"/>
                                        <Button Content="XÓA" Style="{StaticResource ModernButton}" Background="#FF5252" Command="{Binding DeleteFieldCommand}"/>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20">
                                <StackPanel>
                                    <TextBlock Text="Thêm sân mới" FontWeight="Bold" Margin="0,0,0,10"/>
                                    <TextBox Tag="Tên sân" Text="{Binding NewFieldName}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>
                                    <TextBox Tag="Vị trí" Text="{Binding NewFieldLocation}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>
                                    <Button Content="THÊM MỚI" Style="{StaticResource ModernButton}" Background="{StaticResource PrimaryBrush}" Command="{Binding AddFieldCommand}"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- Services Management -->
            <TabItem Header="Dịch vụ">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="10">
                        <DataGrid ItemsSource="{Binding Services}" SelectedItem="{Binding SelectedService}"
                                  AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0" Foreground="White">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Dịch vụ" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Giá" Binding="{Binding Price, StringFormat={}{0:C}}" Width="*"/>
                                <DataGridTextColumn Header="Tồn kho" Binding="{Binding Quantity}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <ScrollViewer Grid.Column="1" Margin="20,0,0,0">
                        <StackPanel>
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                                <StackPanel>
                                    <TextBlock Text="Nhập kho" FontWeight="Bold" Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding SelectedService.Name, FallbackValue='Chưa chọn'}" Margin="0,0,0,10"/>
                                    <TextBox Text="{Binding NewServiceStock}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>
                                    <UniformGrid Columns="2">
                                        <Button Content="LƯU" Style="{StaticResource ModernButton}" Command="{Binding UpdateServiceStockCommand}"/>
                                        <Button Content="XÓA" Style="{StaticResource ModernButton}" Background="#FF5252" Command="{Binding DeleteServiceCommand}"/>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- Orders Management -->
            <TabItem Header="Đơn hàng">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="10">
                        <DataGrid ItemsSource="{Binding Orders}" SelectedItem="{Binding SelectedOrder}"
                                  AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0" Foreground="White">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã ĐH" Binding="{Binding Id}" Width="60"/>
                                <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalAmount, StringFormat={}{0:C}}" Width="*"/>
                                <DataGridTextColumn Header="PTTT" Binding="{Binding PaymentMethod}" Width="*"/>
                                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <StackPanel Grid.Column="1" Margin="20,0,0,0">
                        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20">
                            <StackPanel>
                                <TextBlock Text="Xử lý đơn hàng" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding SelectedOrder.Id, StringFormat='Đơn hàng #{0}', FallbackValue='Chưa chọn'}" Margin="0,0,0,20"/>
                                <Button Content="HOÀN THÀNH" Style="{StaticResource ModernButton}" Background="{StaticResource PrimaryBrush}" Command="{Binding CompleteOrderCommand}" Margin="0,0,0,10"/>
                                <Button Content="HỦY ĐƠN" Style="{StaticResource ModernButton}" Background="#FF5252" Command="{Binding CancelOrderCommand}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Bookings Management -->
            <TabItem Header="Lịch đặt sân">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="10">
                        <DataGrid ItemsSource="{Binding AllBookings}" SelectedItem="{Binding SelectedBooking}"
                                  AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0" Foreground="White">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Khách hàng" Binding="{Binding UserName}" Width="*"/>
                                <DataGridTextColumn Header="Sân" Binding="{Binding FieldName}" Width="*"/>
                                <DataGridTextColumn Header="Ngày" Binding="{Binding BookingDate, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
                                <DataGridTextColumn Header="Giờ" Width="*">
                                    <DataGridTextColumn.Binding>
                                        <MultiBinding StringFormat="{}{0:hh\:mm} - {1:hh\:mm}">
                                            <Binding Path="StartTime"/>
                                            <Binding Path="EndTime"/>
                                        </MultiBinding>
                                    </DataGridTextColumn.Binding>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <StackPanel Grid.Column="1" Margin="20,0,0,0">
                        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20">
                            <StackPanel>
                                <TextBlock Text="Xác nhận lịch đặt" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding SelectedBooking.UserName, FallbackValue='Chưa chọn'}" Margin="0,0,0,20"/>
                                <Button Content="XÁC NHẬN" Style="{StaticResource ModernButton}" Background="{StaticResource PrimaryBrush}" Command="{Binding ConfirmBookingCommand}" Margin="0,0,0,10"/>
                                <Button Content="HỦY LỊCH" Style="{StaticResource ModernButton}" Background="#FF5252" Command="{Binding CancelBookingByAdminCommand}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
