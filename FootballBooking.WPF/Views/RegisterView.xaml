<Window x:Class="FootballBooking.WPF.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FootballBooking.WPF.Views"
        xmlns:vm="clr-namespace:FootballBooking.WPF.ViewModels"
        mc:Ignorable="d"
        Title="Đăng ký - Football Booking" Height="700" Width="450"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        WindowStyle="None"
        AllowsTransparency="True">

    <Border CornerRadius="20" Background="{StaticResource SurfaceBrush}" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Color="Black" Opacity="0.5" ShadowDepth="0"/>
        </Border.Effect>

        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Close Button -->
            <Button HorizontalAlignment="Right" VerticalAlignment="Top" 
                    Content="✕" Background="Transparent" Foreground="{StaticResource MutedTextBrush}"
                    BorderThickness="0" FontSize="18" Click="CloseButton_Click" Cursor="Hand"/>

            <!-- Header -->
            <StackPanel Grid.Row="1" Margin="0,0,0,30">
                <TextBlock Text="Đăng ký" FontSize="28" FontWeight="Bold" 
                           Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="Tạo tài khoản mới của bạn" FontSize="14" 
                           Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>

            <!-- Form -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Họ và tên" Style="{StaticResource BaseText}" Margin="0,0,0,8"/>
                    <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

                    <TextBlock Text="Email" Style="{StaticResource BaseText}" Margin="0,0,0,8"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

                    <TextBlock Text="Số điện thoại" Style="{StaticResource BaseText}" Margin="0,0,0,8"/>
                    <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

                    <TextBlock Text="Mật khẩu" Style="{StaticResource BaseText}" Margin="0,0,0,8"/>
                    <PasswordBox x:Name="PassBox" Style="{StaticResource ModernPasswordBox}" PasswordChanged="PassBox_PasswordChanged" Margin="0,0,0,15"/>

                    <TextBlock Text="Xác nhận mật khẩu" Style="{StaticResource BaseText}" Margin="0,0,0,8"/>
                    <PasswordBox x:Name="ConfirmPassBox" Style="{StaticResource ModernPasswordBox}" PasswordChanged="ConfirmPassBox_PasswordChanged" Margin="0,0,0,10"/>

                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF5252" FontSize="12" 
                               HorizontalAlignment="Center" Margin="0,0,0,15"
                               Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}, FallbackValue=Collapsed}"/>

                    <Button Content="ĐĂNG KÝ" Command="{Binding RegisterCommand}" Height="45" Margin="0,10,0,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ModernButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                        <Setter Property="IsEnabled" Value="False"/>
                                        <Setter Property="Content" Value="Đang đăng ký..."/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <TextBlock Text="Đã có tài khoản?" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center"/>
                <Button Content="Đăng nhập" Background="Transparent" BorderThickness="0" 
                        Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" 
                        Margin="5,0,0,0" Command="{Binding BackToLoginCommand}" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
