<UserControl x:Class="FootballBooking.WPF.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <Border Width="80" Height="80" CornerRadius="40" Background="{StaticResource PrimaryBrush}" Margin="0,0,20,0">
                    <TextBlock Text="U" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding UserName}" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Khách hàng thân thiết" Foreground="{StaticResource MutedTextBrush}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <TabItem Header="Lịch sử đặt sân">
                <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="10" Margin="0,10,0,0">
                    <DataGrid ItemsSource="{Binding MyBookings}" AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0" Foreground="White">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Sân" Binding="{Binding FieldName}" Width="*"/>
                            <DataGridTextColumn Header="Ngày" Binding="{Binding BookingDate, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
                            <DataGridTextColumn Header="Giờ" Width="*">
                                <DataGridTextColumn.Binding>
                                    <MultiBinding StringFormat="{}{0:hh\:mm} - {1:hh\:mm}">
                                        <Binding Path="StartTime"/>
                                        <Binding Path="EndTime"/>
                                    </MultiBinding>
                                </DataGridTextColumn.Binding>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
            <TabItem Header="Đơn hàng của tôi">
                <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="10" Margin="0,10,0,0">
                    <DataGrid ItemsSource="{Binding MyOrders}" AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0" Foreground="White">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã ĐH" Binding="{Binding Id}" Width="60"/>
                            <DataGridTextColumn Header="Ngày" Binding="{Binding OrderDate, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
                            <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalAmount, StringFormat={}{0:C}}" Width="*"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
