<UserControl x:Class="FootballBooking.WPF.Views.CartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FootballBooking.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Giỏ hàng của bạn" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

        <!-- Cart Table -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="10">
            <DataGrid ItemsSource="{Binding CartItems}" SelectedItem="{Binding SelectedItem}"
                      AutoGenerateColumns="False" Background="Transparent" BorderThickness="0"
                      RowBackground="Transparent" GridLinesVisibility="None" IsReadOnly="True"
                      Foreground="White" FontSize="14" ColumnWidth="*">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#2A2A2A"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#333333"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Sản phẩm / Dịch vụ" Binding="{Binding ServiceName}" Width="2*"/>
                    
                    <DataGridTemplateColumn Header="Số lượng" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="-" Style="{StaticResource ModernButton}" Width="30" Height="30" Padding="0" Margin="5,0"
                                            Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"/>
                                    <TextBlock Text="{Binding Quantity}" VerticalAlignment="Center" FontWeight="Bold" Width="30" TextAlignment="Center"/>
                                    <Button Content="+" Style="{StaticResource ModernButton}" Width="30" Height="30" Padding="0" Margin="5,0"
                                            Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Đơn giá" Binding="{Binding Price, StringFormat={}{0:C}}" Width="*"/>
                    
                    <DataGridTemplateColumn Header="Thành tiền" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TotalPrice, StringFormat={}{0:C}}" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="🗑" Background="Transparent" BorderThickness="0" Foreground="#FF5252" FontSize="18"
                                        Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="30" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <StackPanel VerticalAlignment="Center" Margin="0,0,30,0">
                        <TextBlock Text="TỔNG THANH TOÁN" FontSize="12" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding TotalPrice, StringFormat={}{0:C}}" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Right"/>
                    </StackPanel>
                    
                    <Button Content="THANH TOÁN" Style="{StaticResource ModernButton}" 
                            Height="50" Padding="40,0" Command="{Binding CheckoutCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Empty Cart Message -->
        <Grid Grid.Row="1" Visibility="{Binding CartItems.Count, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter='0'}" d:Visibility="Collapsed">
             <!-- Placeholder for Empty Message -->
        </Grid>
    </Grid>
</UserControl>
